# Creating an Interactive Avatar Builder with Rive and React (36:45)
https://www.youtube.com/watch?v=Qr7Ng6fpqnk
YouTube 自動英文字幕（yt-dlp 2026-09-23 抓取，已去重）

[00:00:00] foreign moving on to part two of The Avatar Creator tutorial where we'll be looking at the implementation of how to actually build out this web application using a nexjs application and the drive react runtime so as you saw in previous Avatar Creator tutorials how this kind of looks is you can choose different facets of your avatar such as the eyes body shape and the hair even and see it kind of reflect in this left panel preview here

[00:00:31] as you choose some of these different options and the nice thing about this in this web application is that it works in this desktop view but also in a mobile view as well so it's a little more responsive when we choose different things here we can still go back to desktop view and if you haven't checked out our react documentation yet for arrive you can go to help.rive.app and see in the runtime section this react section here you'll

[00:00:56] see some quick start guides and getting started guides on how to easily integrate drive into your react-based applications so let's get started so just as a recap from the previous videos let's take a quick look into the ride file to see how this is structured and how we'll be using it in our next JS application so we've got kind of three main artboards that we're going to be working with the first one is the Avatar this

[00:01:18] correlates to the main preview window that you're seeing here and we've also got this these different icon artboards so we've got body color icon body size icon body eyes icon and so forth and so these will correlate to the different icons that you see here in this top bar when choosing which icon you want to change the feature of and then lastly we have this body color button body size button body eyes button very similar to the body icon button but

[00:01:48] you'll notice that you know in each of these sections we have multiple of these buttons but we only see one button here and we're going to be making different instances of arrive in this case making multiple copies of the artboard you could imagine as well as their state machines to basically replicate all these different options on on your application and so depending on how many options there are for the body color that's how many copies of the body color

[00:02:15] we're going to make here so in this case five and so we'll see what that looks like in code in a bit but the nice thing about this is that it keeps the file size down you don't have to make an artboard and a state machine in all different animations for all the different possibilities of options that go into creating the Avatar you only have to make one of each and we'll see why in a bit but it also makes the state machine practical so as an implementer

[00:02:41] don't have to go in and try and create an instance and rename each instance of arrive with a different artboard name and a different state machine name I can reuse the same artboard name when I'm making multiple copies of these things in code and if the naming of the artboard name and the state machine name and the state machine inputs are pretty predictable in terms of the designer and myself collaborating and making a good pattern

[00:03:07] of how we're going to be reusing this artboard across multiple instances of Drive in code it makes it a lot easier to to create these patterns at runtime now I wanted to quickly go over how to export your Rye files into your next JS application so we'll go into the share button here and then this download fly out you'll see this for runtime.rav button here so I'm going to click that and you'll see it downloads it to my Chrome browser here

[00:03:36] and I've already got this set up but you can drag it into your next JS application and for the purposes of this tutorial I've already scaffolded this Avatar Builder project using the create next app starter CLI tool which scaffolds a whole next JS application with the latest version of next.js it's also set up Tailwind CSS which is some the the tooling we'll be using to build out the uh the UI and the CSS for this project

[00:04:03] among some other tooling that I don't need to fuss with but you'll notice here I've got this public folder and this is where all my public accessible assets are going to live so you'll see some SVG files you can put in your favicon files and in this case we're going to include our drive files the dot RAV extension ones in the public folder as well because this will be publicly accessible whenever I deploy this application to the internet and that's what I want my

[00:04:28] runtime to look for and you'll see why that is in a bit but it's certainly not the only way you can import Rye files into your next JS applications you could also deploy your dot Riv files onto a CDN for exam people like AWS S3 or Google bucket and pull in the right file via the a URL as well so there are different ways to import the right files and you can just check the documentation on which way might work best for you now looking at some of the components of

[00:04:57] this project if we drill down into this you'll notice we have a components folder this is where I'm going to be storing some components that we'll be using throughout the application we've got this Avatar component icon buttons the containers that generate all the icons and things as such and we'll be looking into kind of how this looks in the bigger picture when we construct our web application a little later but we've also got this app folder where

[00:05:24] we've set our context and this Avatar State file now I for this application want to set up this kind of global context state so that it can be shared across all components but also any component in my react hierarchy tree can update the global State as needed so that the other components around the reactory can get those latest values and you'll see kind of why we need that to happen but you know when you can imagine when a a user is clicking on a certain

[00:05:53] mustache for the facial hair we want the Avatar component that's completely unrelated to that button option to know when that mustache is chosen so that it can reflect having a mustache chosen in the preview window and so this is why we'll need a global state now you'll see here in this Avatar State file I've got this initial State set up and it's just kind of a big object we've got this active icon string here and it's referring to part of an artboard

[00:06:21] name and this will refer to the actual icon that's chosen so if we're looking at facial hair options we've got the facial hair active icon set or if we're looking at background color the background color active icon will be set and so this kind of correlates to the naming of the artboard for body color icon and Body Face hair icon and things like that and then we've also got this object mapping here of Rive Avatar selections

[00:06:48] where we've got a mapping from a string called body color body size to some number and if you recall in the right file these different options will correlate to the button options so the color the size the eyes that all makes sense but the number it refers to a number input in each of those option buttons called num option and the num option for zero for one for two etc for each of those buttons correlate to a different option

[00:07:21] value so zero might be mustache one might be hairless facial features and three two might be referring to a beard things of that nature and so each number has significance here for each of these the the body color the size the eyes and so forth and so we want to store that mapping of what the user has chosen in each of those option grids containers back into the Central State so that the Avatar preview can read those new values and reflect the correct Avatar

[00:07:53] accordingly so this is why we have set up our state that way and we're going to be using a reducer pattern to essentially make it easy for components to change the state the global state in a way that it's safe and this is an easy testable pattern you can read more about the use reducer and the use context hooks in the react documentation but we've got these two functions here set active icon and set Rive Avatar selection so and set active

[00:08:24] icon is called this is when a user clicks on different icons in the icon bar and the set arrive Avatar selection is when an option is selected for a given icon and then we kind of wrap this in this provider component and this is kind of what wraps the entire react tree so that any component in under it can cause some of these helper functions to change State accordingly and the last thing I wanted to look at is this Avatar config.json file and so

[00:08:52] if you recall in the option button State machines in the right file we had a number input for the state machine called num option and this num option could have a value for zero to even five six seven eight whatever it might be depending on how many options there were for a given character trait for example in body color there were about five options and in the eyes there were four options and so forth and so we create this Json file in code just to

[00:09:21] keep track of how many option values there are and how many copies of the option buttons that we need to create to create that grid of options that the user can choose from so we have this Json code fig here and we're going to be reading this in as we kind of load up the next JS application and pass that pass this data down to our components so it knows how many i options and size buttons and so forth to create when we render that out on the UI

[00:09:50] and so taking a bit into the hierarchy here we're starting off with our page component or page.tsx file with this home component here and you'll see everything's kind of wrapped in this main block I'm going to ignore a lot of this Tailwind CSS syntax here that you see in a lot of the class names for this project but you'll also see this Avatar State provider that's wrapping this main entry component this provider is what provides

[00:10:14] the global State and the actions to update the state to all the components below it and then this drive main entry is where we'll kind of dig into but you'll notice that we're passing in this local data block this local data block is that Json file essentially that we went over that dictates how many options there are for each of the character or Avatar traits here so this is important when we figure out how many buttons to generate and

[00:10:38] things like that but moving on to the arrive main entry component you'll see there's a lot more divs and a lot more tags here that are dealt with basically maintaining the layout and style of this whole application and so I'm going to avoid a lot of the style talk in here but you can peruse the Tailwind CSS docs and also the source code at another time really what I want to start with is this

[00:11:02] drive Avatar component so if we dig into revive Avatar you'll see here that we are importing at the top the react runtime and we're going to be importing some relevant Hooks and some other enum values that we're going to be using when we instantiate drive so with this right Avatar component we are using the or calling the use Drive component uh hook here and providing some relevant details for what to render

[00:11:28] essentially onto a canvas and so you can see we're giving it the name of the Rye file at the path where our publicly accessible asset is going to be so because that public folder is going to expose all the assets at its root I'm giving it a basically shallow relative path to the root of the application and thus the Avatar demo.rav file I'm also passing at the artboard name here and the name of the state machine

[00:11:54] in this case I made it a constant um celaya params and basically telling it to autoplay to true I'm also pulling in the context here which is basically going to give me the global state of everything um in this application and this is going to be relevant because when I choose different options for some of these Avatar traits I want the Avatar character here to know what those are so that it can reflect that accordingly and

[00:12:21] here I'm basically pulling in a reference to all the state machine inputs that go into making the state machine and you'll notice that there's State machine inputs for the body color the body size and the values for these number inputs here will actually correlate to the number value that is selected for a given trait here so if this is zero or if this is 2 and I click two here this should update the num body color to a value of 2 as well and that's

[00:12:49] why we kind of have this use effect block down here and what this is doing is it's looking for all these values from Drive Avatar selections which comes from the global state um and it's trying to read the latest values for each of these different Avatar traits and set these on the number input accordingly so that the Avatar can update and so this use effect is essentially just looking for those changed values

[00:13:15] and the other thing that it's doing is when it notices a changed value and executes this use effect Block it's calling this changes trigger.fire and this state machine has an extra trigger input that when fired kind of gives it an extra bounce to visually indicate to the user like we saw in the previous videos that a change has happened so um there's that little bounce there and then we're rendering the Rive

[00:13:39] component down here in the return statement but going back to the right main entry one other thing I want to call out is for the desktop views I'm setting an aspect ratio of one to one entailment that's dictated by aspect Square and the reason for that is because the artboard size and the Rye file for this Avatar artboard is actually a one-to-one aspect ratio and so it's definitely important to make a container when

[00:14:06] rendering the Rye file of the similar aspect ratio and sometimes that makes a difference sometimes not but in this case in order to kind of lay things out accordingly this is important and so giving it an aspect ratio here made the most sense for figuring out the scale of its width and height now let's take a look at the icons here and so with icons we're going to be looking at this top bar that dictates which icon and which pair of options

[00:14:32] we're going to be looking at so if we go back to our arrive main entry components we've already looked at the drive Avatar bit now we're going to go down to this lower Block in this render part of the function and look at this icon container block we're going to ignore the drive options container for a second here but going into this arrive icons container you can see it's a separate component that we've created and it's pretty basic it's very small

[00:14:59] it's called Drive icons container and it's just got a div that's containing basically all these different options and you can see that we've hard-coded about what is this six different icons here that each correlate to the different Avatar traits and so because there are only six it makes it pretty easy to just hard code this in we don't have to be smart about programmatically trying to generate these but of course you could probably get pretty clever

[00:15:23] with the name with the way that you set up the state machines set the names and things like that what we're doing here is we're creating a Rive icon button for each Avatar trait and we're passing in this artboard um basically dictating uh this for this reusable component Drive icon button you know which artboard should this icon render and so we're doing it for the color the eyes the size and things like that and raising this helper function to

[00:15:50] basically extrapolate the exact artboard name that we want to uh to render and so if we pass in color we want to make sure that this is actually body color icon as the artboard name we want to render but in total really the takeaway here is that we are reusing the survive icon button component here to render out all the different icons and that we don't have to have a specific react component for each of the different character traits we can just reuse one which is

[00:16:18] the nice power of react if we dive into the drive icon button you'll see that it looks kind of similar to the arrive Avatar component that we looked at earlier in that yes we're importing the react canvas runtime and those hooks that seem familiar they use drive and use State machine input and so again we are pulling in the right file the artboard name and this time we're not explicitly calling out the artboard name um we are taking it from the props here

[00:16:44] the artboard name that we passed in from the the parent component and we are also passing in the name of the state machine which is again a constant and we are disabling the listeners here and in the right file it actually has listeners so that at design time you could see the interaction of what happens when you click on the button and things like that we're going to disable that because we're going to set that up all programmatically for accessibility

[00:17:05] purposes now the next thing I want to look at is this variable that we're creating here called strip down name now if you remember we passed in this component the artboard name right and we for example in this case this would be body color icon and what we want to do is we want to strip out that icon part from the string so that we're just left with body color now the reason we want to do that is because we use body color in the

[00:17:31] state machine input name for Num body color for the icon and that way we can set the actual value for the icon here and so we're manipulating that state machine input we're creating a reference to it here called num option when we use the use State machine input to grab a reference to that state machine input we are essentially using the num and then the strip down name so this is using some string interpolation to essentially get a num body color essentially when we

[00:18:00] create references to these this is why we need the strip down name we don't want the icon part in there now I'm going to go down to this use effect where we look at the num option and so in this use effect what we're doing is we're setting the num option for the icon which is going to dictate uh basically what selection is reflected from the user's Choice here so if they chose purple uh this icon should reflect purple and so this num option here is

[00:18:28] being updated with that latest value and that's because it's reading from again we have Global State pulling in from that use context here and reading the latest value that was chosen by the user from the options and so that options is stored as a number in the mapping in our Global State and we are just updating the num input here for the icon in its state machine to reflect that new value now just to show you kind

[00:18:53] of what this looks like without this use effect if we didn't listen to the global State and we didn't set that num option input for this specific input in the state machine you would see that as we choose different options here it Remains the Same value at whatever it was set either initially when the state machine is run or when the user lasts select set that value and so for this reason because we

[00:19:17] are changing the options here we need this use effect to basically listen for changes in those values so that we can accurately reflect the color changed here we also want to have this to use effect below it to look at the strip down name because we want to see is this icon actually active or not again because we're using a generalized component for all icons we need to make sure that we set whether or not a given icon is

[00:19:43] active based on whether or not the user has selected it and by active I mean given this kind of Border you can see a bit here if I zoom in a bit but you can see that it's got this kind of Halo background a bit outline to dictate that hey this is the active icon of options that we're looking at and we are just setting a state machine input for that for each of the state machines here um to off or false sorry false false false and for the one that I selected as

[00:20:12] true so we are essentially using this use effect to listen to whatever the user has selected there for the is icon active State machine input and we're going to be using react callbacks here the use callback hooks here to essentially coordinate some of the smaller interaction pieces manually so things that were set up in the right file as listeners for hovering and clicking interactions we're going to be

[00:20:36] ignoring those as we called the should disable listeners flag above in the use Drive hook and we're going to be used manipulating those State machine inputs manually so you'll see we have a reference to the is hovered State machine input here and we're setting it to true or false whether or not we blur on it and giving it a better accessible experience that way you'll notice that we've wrapped the Rive component which is the icon in a button component and so

[00:21:00] this gives us a nice way to get some easy accessibility features just by wrapping it in a button and the last thing I want to mention is that on click of a certain icon we are not only giving it the active style here we're also calling this set active icon function with the artboard name that is given here so in this case Body eyes icon and the set active icon comes from an exposed function from our use context that we called above and again how I

[00:21:29] mentioned earlier we are exposing not only the global state but in a way to easily update the state from any given component this is one of those functions that allows us to update from this particular component which icon is being actively looked at so that later on we can look at the different options in this grid here for the given icon that was tapped and so this is important for setting the context of you know what was chosen and now the next thing we want to

[00:21:57] look at are the actual option button components here in the container that shows the options in a grid kind of format and you'll notice these are kind of dynamic in the number of options per Avatar feature here and so we'll take a look at how that happens and so at the top of the main entry we're again pulling in our Global State this time to look at the Active icon the active icon is again either like body color or body eyes it's a string that

[00:22:24] dictates the icon of the trait that the user wants to select an option for and what we want to do here is we want to essentially trim down that string to the Accord trait that we're looking at so for example with body color we want to trim out the body string here and just get the string color or for body eyes icon we want to trim out the body part and just get eyes and if it's just background color this is a special exception we'll just take

[00:22:54] background color now why we want to do that is because if we look into our local data we are this is again the config that dictates how many options there are per Avatar trait so for color there are five options and things like that and we want to essentially get for each of the active which is the active icon which is eyes or color whatever and just get the key mapping for this uh the string here and what we're going to do with that is

[00:23:22] at the very bottom of our return function in our arrive options container which we're going to take a look at you'll see that we're taking that trimmed active icon so this would just be color size eyes as you can see here in this preview and just trying to get that simple string we're also going to be looking at that Json data and trying to find the mapping of how many options there are for the given chosen option so if the user has

[00:23:49] chosen for example facial hair they'll get face hair as the trims icon string and get the num options and set three for the prop for this num options prop and so again this would be face hair and this would be three essentially so we were just trying to Pare down the data too what is the active icon and trying to map that to our hard-coded Json data to see how many options this drive options container needs to generate um dynamically for this grid

[00:24:20] so let's move into the arrive options container all right so moving into the arrive options container component you'll see this looks somewhat similar to the arrive icons container component and the arrive icons container component though we did not dynamically generate however many options or icons there were we knew there were a fixed number of Avatar traits there were six of them so we hard-coded six Rive icon component

[00:24:44] buttons for this container in this drive options container though again we are taking those props of what is the actual thing that we are wanting to generate uh that the buttons for in this case face hair and how many options there are from our Json data again that would be three and we are taking those Collective traits and props that are passed into this component and essentially generating dynamically these arrive option button components that dictate

[00:25:13] each of these different options here and so essentially that's all this container component is doing it's dynamically creating some of these Drive option buttons based on the naming and the pattern of how we named the artboard how things are named in our Global State and things like that so you can take a deeper look into the state code and how things are updating and how things are parsed here but this is essentially all this container is is it's generating

[00:25:37] buttons now looking at what this is generating for each button it's this arrive option button component and we are passing it three props one is the key which is a standard in react when you're iterating and creating multiple components and we're also passing in the artboard name which is calling this helper function to essentially format the artboard name that we want to render and then we're also passing in this option

[00:26:01] idx or short for index prop here with I and I is coming from this Loop that we're iterating over from 0 to the number of options that we need to generate and we're going to be using this to actually dictate the number option that each button should display for Num option that number input in the state machine for this artboard so again we're only using one artboard here which is in this case this would be Body Face hair button option button and the state

[00:26:29] machine has a num option input that goes from that's enumerated from zero to however many number options there are for a given Avatar trait for example in body hair option button artboard here there are about 11 different options here and so each one is going to have its own option idx of 0 1 2 3 and so forth until it gets to the end and again that's dictated by the number of options that were passed into the container for the chosen icon now moving on to the

[00:26:58] Rive option button component you'll see here we're going over we're getting these props again the artboard name which is going to determine which artboard we're going to render and the option idx prop which is a number that dictates what number each arrive instance is given when rendered and it's going to map to that num option input in the state machine when for these option button artboards and so starting off again this looks

[00:27:23] very similar to how we instantiated the Avatar and also the icon we are pulling in the react canvas in runtime and importing the use drive and use State machine hooks and we are calling in that drive Source here from the Avatar demo.riv file the artboard name given from the props and the state machine name which is also again a constant we are setting autoplay to true and then we are again disabling the listeners

[00:27:52] that are set on the buttons here because again we want to provide a more accessible experience by manipulating the state machine input directly and you'll see down below when we return what we return from this component is a button wrapping that drive component so again we are using the accessibility features of being wrapped inside of a button to be able to handle you know a lot of things such as tab accessibility and things like that starting from the

[00:28:18] top what we want to do is look at this main name variable we are essentially taking the artboard name and replacing the word button from it again you can imagine each of these button components is one of these is what we're rendering this would be body color button and we want to remove the buttons part of the string so we're just left with body color and the reason we're going to do that is because we're going to be using that as

[00:28:43] the key for uh traversing our Global state so for body color when we look back at our Global States we're just looking at the body colors thing and we are also just looking at the value that it is set to in our state as well so going back here and toggle that off we just want to get the selection value of what was chosen before so for example if we went back to the hair you can see that this value would be set to 2 because it's starting

[00:29:10] from 0 1 2. similarly with the background color you can see that this is slightly uh set to active and so the value here would be 0 1 2 as well for the body color the background color now moving down along the lines we'll see we have similar functions for the on focus on blur and on click callbacks that we attach to our button here and these just kind of help dictate you know it is a button hovered is a thing clicked and setting its kind of active

[00:29:41] State here you can see it's a little darker now when you click on one of these buttons you'll see if we look into this on click function we're calling this set arrive Avatar selection and if you remember set arrive Avatar selection comes from our use context hook here and this is one of those other functions that updates our Global State just like we had in icon we had set active icon function that we called on

[00:30:08] the on click function for each of the icons to dictate you know which icon we're going to be looking at and similarly for these buttons when you click on one we're going to be calling the set revive Avatar selection button with the name of uh what we're updating so in this case Body color and the actual value correlated to which option was selected so in this case it would be 0 1 2 3. and so here this is how we kind of make

[00:30:36] the magic happen of updating the global state of which trait selection was made by a user so whenever a user clicks one of these options that value is updated in our Avatar state for this body color here and then this is Red from the Avatar component to update accordingly and again give it that bounce to show that something updated so that's kind of the the magic of it all of how updating Global state in one

[00:31:01] component can lead to making a change not only in this actually Avatar component here but also in this icon over here you can see it reflecting the the change when we Traverse all the different colors and options here and you can see here above we are also just setting is icon active for any given button based on whether or not it Maps the selected value so for example if we go to the hair icon here you'll see that it's not just the first button that's

[00:31:31] highlighted as you would expect like at the default it's the actual hair icon that um it was selected previously that is highlighted and so you'll see is icon active we have this fourth parameter to use State machine input and this is kind of what you can set as a default value when the the arrive instance is made and when the state machine is set up and so this will evaluate to a true or false value here basically determining whether

[00:31:57] or not the actual value for a given button should be highlighted or not as it's creating all these different buttons based on if it's selected in the state and similarly you can see in this use effect just below it we are setting the is icon active.value for any given button when a selection is clicked and so you can see here it's iterating over each of these buttons and seeing hey is this correlated to The Chosen option and

[00:32:23] if not don't just set the value to false or keep it as it is and if I choose a new Option also that is active icon value to true and for the sizing of it you'll notice that the size of the button is not directly a one-to-one aspect ratio as you can as you saw for the icon and this Avatar preview it's a different aspect ratio but again we're taking that into account as we're creating the CSS values for the button and the right component

[00:32:53] will scale with the button parent component and so we don't have to worry about setting any class names on the drive component here another thing to call out is this line here for these few lines here for the num option variable what we're doing is calling that use State machine input for the num option reference so the num option State machine input we want to grab a reference to and we're setting a fourth parameter here with option idx

[00:33:20] which is again the number that we want to set the state machine input value to so as we're iterating and creating each of these buttons for example with the hair option we're given a iterating 11 times to create each of these buttons and each button is going to be given an option ID X of 0 1 2. and as we're creating these buttons when it goes to create the state machine and get a reference to the num option input it's setting each of these to default values

[00:33:48] of 0 1 2 Etc et cetera et cetera and that's how we enumerate all the different hair options all the different eye options just based on that Json data file that we created in the project that dictates how many options to create per trait for the Avatar and that's kind of how we enumerate all the different buttons here by using this use State machine input and setting the num option for the state machine to a different value every time we iterate and create

[00:34:16] one of these Drive option buttons now just to show you kind of what would happen if we didn't instantiate a default value for this num option variable what would happen is if we took out this fourth parameter for option idx note that the first and last colors here are this blue and purplish color now if we took away that default value reset this you'll see we actually only render the last value for each of these these

[00:34:45] buttons and same thing with all the rest of the icons we're only showing off the last option uh as as a value and the reason for that is because the state machine is never instantiated with a number option to show um when it's choosing all these things and so it's important to kind of instantiate sometimes the default value especially if you're iteratively enumerating different artboard uh Drive instances drive

[00:35:11] artboards and things like that in this kind of manner when you're generating multiple instances and similarly for the is icon active variable here for the use State machine input if we did not provide a fourth parameter what would happen is that the active icon would be reset every time we Revisited an icon and it wouldn't remember exactly which button or option to activate as the active selected one and so this is why it's important to

[00:35:36] sometimes default and keep track of the state of you know what things were selected especially in the kind of scenario like this use case and that's the Avatar Creator you'll find links to the source code that you can peruse on your own time in the description box so you can check out all the extra details such as the styling how we made things responsive in the mobile view and other concerns like that now you could take this even a step

[00:35:58] further if you were to Fork this project and actually include it in your own applications right here it's kind of just a prototype you can of course you know change different traits of the Avatar and that's all fine and dandy but you can make this a lot more interesting if for example you captured the state the global state of what was chosen and selections for this Avatar and perhaps store that in a database for a given profile user so that you could use the

[00:36:21] Avatar to actually build an actual profile avatar creator for your product and so that's just one way you could kind of extend this prototype to integrate it into your own applications as well if you have any questions or concerns about the implementation or even how things are built in the drive editor feel free to join our Discord Community to leave a comment on this and thanks for choosing Rife
