# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# dataconnect generated files
.dataconnect

# macOS metadata
.DS_Store
**/.DS_Store

# GA4 / Firebase 匯出資料（含玩家城市分布等敏感統計，不公開到 repo）
*.csv
*.xlsx

# Claude Code 工具產物（本機 worktree / 設定，不入庫）
.claude/

# 含玩家地理/留存敏感統計的策略分析（同上方 *.csv/*.xlsx 原則，不上公開 repo）
靈修冒險-多領域專家分析.md

# GCP / GA4 服務帳號金鑰（具體檔名排除，避免 *.json 誤排 firebase.json / package.json）
ga4-key.json
# 管理員 OAuth 一次性授權 token（代表 James 本人授權，用完即收，絕不進 git）
token.json
