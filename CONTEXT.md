# 靈修冒險 · 領域詞彙表

> 專案術語的單一正本：只定義「這個詞在本專案指什麼／不是什麼」，不含實作細節。
> 機制與流程見 `CLAUDE.md`，決策緣由見 `docs/adr/`。輸出（issue 標題、提案、測試名）請用這裡的詞，不要漂移到同義詞。

## chapterKey

一章靈修內容的唯一識別鍵，也是玩家完成紀錄的主鍵。使徒行傳用數字（1–28），其餘書卷用「前綴＋章號」字串（如 `GEN3`、`COR2_6`、`TH1_4`），完整前綴表見 CLAUDE.md「章節 key 命名規則」。
**不是**書卷 key（那是 `GEN` 這種不帶章號的），也**不是**日期——同一個 chapterKey 可因補讀、提前靈修出現在任何日期。

## 合併日（雙章呈現）

教會進度一天讀兩章的日子（全年 19 處）。SCHEDULE 該日陣列 length ≥ 2；玩家進「合併日選擇頁」自選先讀哪章，兩章各自有完整內容與獨立獎勵，任一章完成即算「今日有靈修」（出席只 +1）。
**不是**把兩章合寫成一份內容（v2.11 前舊做法已翻轉作廢），也**不會**讓當天靈修天數加兩次。

## mergedActive

BOOKS 各卷上的過渡旗標：`true` 代表該卷章節物件未補齊、仍用舊的 merged 倍數統計。現況（2026-08-31）：25 卷全為 `false`，過渡已完成，此旗標是待退役的歷史遺留。
**不是**「這卷有沒有合併日」的開關——合併日由 SCHEDULE 陣列長度決定，與此旗標無關。


（2026-09-01 更新：本機制已退役——欄位與雙路徑已自 content.js／core.js 移除；此詞條保留供閱讀舊文件時對照。）
## 曠野呼聲

玩家留言與管理員多輪對話的回饋系統。每個 thread 走四狀態狀態機 `new / awaiting_admin / awaiting_player / closed`（closed 為終態，30 天無動靜自動關閉），單一正本在 `shared/feedback-schema.js`。
**不是**公開留言板——留言只有本人（依 uid）與 admin 讀得到，匿名留言連本人都讀不回。

## 閱讀勳章

玩家表示「已讀完本章經文」後獲得的 +15 XP 標記，鼓勵先讀整章再玩；來源分 `bible_com`（點連結去讀）與 `already`（已用紙本／其他 App 讀過），是閱讀率指標的口徑基礎。
**不是**完成靈修的必要條件，也**不是**查核機制——玩家自己說讀過就算。

## 出席燈 vs 累積靈修天數

「🔥 出席燈」＝右上角的當日狀態：今天完成任一章就亮、沒完成是暗的。「累積靈修天數」＝旁邊的數字：完成靈修的總天數，中斷不歸零。程式變數名 `state.streak` 是歷史遺留，語意已是「累積」而非「連續」。
**不是**連勝（streak）機制——沒有斷簽懲罰、不歸零，文件與文案不再寫「連續 N 天」。

## 雷區章

語氣指南點名、錯的方式很微妙的敏感章節（暴力、性、審判題材，如創 19 所多瑪、創 22 獻以撒）。內容產線規定雷區章由高階模型寫、主編另審；基調正本在 `content-tone-guide.md`。
**不是**「很難讀懂的章」（那由導讀的 hard 難章提示處理），也**不是**可以跳過或淡化的章。

## 稱號

第五個裝備部位（2026-08-30 起），綁「累計完走書卷數」的 9 級階梯（終點 66 卷），到里程碑解鎖、在衣櫃換上。
**不是**每卷各發一個的專屬稱號——③c 已砍（會與累計階梯語意打架），完走單一卷不會拿到該卷專屬稱號。
